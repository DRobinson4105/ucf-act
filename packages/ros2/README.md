# ROS 2 Workspace

## Installation

From the workspace root, run:
```bash
bash install.sh
```

This installs ROS 2, required system packages, and builds the workspace.

## Build

After making changes, rebuild with:
```bash
colcon build
source install/setup.bash
```
