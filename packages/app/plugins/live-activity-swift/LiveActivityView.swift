import SwiftUI
import WidgetKit

// This file is intentionally minimal — the lock screen banner is rendered by
// RideBannerView inside LiveActivityWidget.swift. Keeping this file so the
// Xcode target's file list stays stable (expo-live-activity plugin references it).
